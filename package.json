{
  "name": "froppo-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "if [ -d frontend ]; then cd frontend && npm ci && npm run build; else echo 'Frontend directory not found'; exit 1; fi",
    "install": "if [ -d frontend ]; then cd frontend && npm ci; else echo 'Frontend directory not found'; exit 1; fi"
  }
}

